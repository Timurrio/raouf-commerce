export interface Iitem {
    id: number,
    category: string,
    img: string,
    description: string,
    price: number,
    otherImgs: string[],
    specs: string,
    texture: string,
    weight: string,
    size: string
}
